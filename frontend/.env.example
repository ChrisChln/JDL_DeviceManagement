# Frontend env is read from the repo root .env via frontend/vite.config.js.
# From the repository root, run:
#   cp frontend/.env.example .env
# Then update .env with your actual configuration values.
